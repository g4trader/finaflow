#!/usr/bin/env python3
"""
Script para testar se o hybrid_app.py consegue ser importado sem erros
"""

import sys
import os

# Adicionar o diretório atual ao path
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

try:
    print("Tentando importar hybrid_app...")
    from hybrid_app import app
    print("✅ Importação bem-sucedida!")
    print(f"App: {app}")
    print(f"App title: {app.title}")
    print(f"App version: {app.version}")
except Exception as e:
    print(f"❌ Erro na importação: {e}")
    import traceback
    traceback.print_exc()
"""
Script para testar se o hybrid_app.py consegue ser importado sem erros
"""

import sys
import os

# Adicionar o diretório atual ao path
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

try:
    print("Tentando importar hybrid_app...")
    from hybrid_app import app
    print("✅ Importação bem-sucedida!")
    print(f"App: {app}")
    print(f"App title: {app.title}")
    print(f"App version: {app.version}")
except Exception as e:
    print(f"❌ Erro na importação: {e}")
    import traceback
    traceback.print_exc()
"""
Script para testar se o hybrid_app.py consegue ser importado sem erros
"""

import sys
import os

# Adicionar o diretório atual ao path
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

try:
    print("Tentando importar hybrid_app...")
    from hybrid_app import app
    print("✅ Importação bem-sucedida!")
    print(f"App: {app}")
    print(f"App title: {app.title}")
    print(f"App version: {app.version}")
except Exception as e:
    print(f"❌ Erro na importação: {e}")
    import traceback
    traceback.print_exc()
"""
Script para testar se o hybrid_app.py consegue ser importado sem erros
"""

import sys
import os

# Adicionar o diretório atual ao path
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

try:
    print("Tentando importar hybrid_app...")
    from hybrid_app import app
    print("✅ Importação bem-sucedida!")
    print(f"App: {app}")
    print(f"App title: {app.title}")
    print(f"App version: {app.version}")
except Exception as e:
    print(f"❌ Erro na importação: {e}")
    import traceback
    traceback.print_exc()
"""
Script para testar se o hybrid_app.py consegue ser importado sem erros
"""

import sys
import os

# Adicionar o diretório atual ao path
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

try:
    print("Tentando importar hybrid_app...")
    from hybrid_app import app
    print("✅ Importação bem-sucedida!")
    print(f"App: {app}")
    print(f"App title: {app.title}")
    print(f"App version: {app.version}")
except Exception as e:
    print(f"❌ Erro na importação: {e}")
    import traceback
    traceback.print_exc()
